<div class="text-xl max-w-[60ch]">
  <dl>
    <dt class="font-bold">Description</dt>
    <dd class="mb-6">{{ item.description }}</dd>
    <dt class="font-bold">Dimensions</dt>
    <dd class="mb-6">{{ item.dimensions }}</dd>
    <dt class="font-bold">Coordinates</dt>
    <dd class="mb-6">{{ item.coordinates }}</dd>
    <dt class="font-bold">Catalog ID</dt>
    <dd class="mb-6">{{ item.catalog_id }}</dd>
  </dl>
</div>
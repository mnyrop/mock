<div class="text-xl max-w-[60ch]">
  <dl>
    <dt class="font-light text-gray-400">Description</dt>
    <dd class="mb-6">{{ item.description }}</dd>
    <dt class="font-light text-gray-400">Dimensions</dt>
    <dd class="mb-6">{{ item.dimensions }}</dd>
    <dt class="font-light text-gray-400">Coordinates</dt>
    <dd class="mb-6">{{ item.coordinates }}</dd>
    <dt class="font-light text-gray-400">Catalog ID</dt>
    <dd class="mb-6">{{ item.catalog_id }}</dd>
  </dl>
</div>